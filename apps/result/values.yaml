replicaCount: 1

image:
  repository: ghcr.io/ybojko/voting-app-result
  tag: v1
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 80
  nodePort: 30081

env:
  DB_HOST: postgres-postgresql
  DB_PORT: "5432"
  DB_USER: postgres
  DB_PASSWORD: postgres
  DB_NAME: postgres

resources: {}
