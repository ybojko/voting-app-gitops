cloudflare:
  email: ""            # Cloudflare account email
  apiToken: ""         # Cloudflare API token (Zone:DNS:Edit + Zone:Read)
  existingSecret: ""   # Name of existing Secret with key "api-token" (set by ESO)

issuer:
  email: ""            # Let's Encrypt notification email
  server: https://acme-v02.api.letsencrypt.org/directory  # production
  # server: https://acme-staging-v02.api.letsencrypt.org/directory  # staging (for testing)
