replicaCount: 1

image:
  repository: 522868276919.dkr.ecr.eu-central-1.amazonaws.com/voting-app-result
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  hostname: result.redf1ag.xyz

env:
  DB_HOST: postgres-postgresql
  DB_PORT: "5432"
  DB_USER: postgres
  DB_NAME: postgres

existingSecret: ""

secretEnv: {}

resources: {}
